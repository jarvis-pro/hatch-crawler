# RFCs

hatch-crawler 的重大架构决策都以 RFC 形式记录。RFC 不是"事后补的设计文档"——它是**动手写代码之前**的提案，落地后只更新状态字段，**不重写历史**，让未来的你能看到当时的权衡。

> 工作约定：动手做任何会跨多个 PR、影响数据模型、影响 Spider / Worker 抽象、或会引入新平台 / 新资源类型的工作之前，**先写 RFC**；RFC 不需要写得完美，但要把"为什么是这条路而不是另一条路"讲清楚。

## 索引

| 编号                              | 标题                         | 状态                                   | 摘要                                                                                    |
| --------------------------------- | ---------------------------- | -------------------------------------- | --------------------------------------------------------------------------------------- |
| [0001](./0001-multi-platform.md)  | 多平台 / 多资源类型扩展      | **Partially Implemented** (2026-05-01) | 多平台（5 个）+ kind 化已落地；4 层抽象（Platform 接口对象）尚未抽出                    |
| [0002](./0002-media-downloads.md) | 媒体下载与转码（含 YouTube） | **Withdrawn** (2026-05-01)             | 早期落地后下线：attachments 子系统已移除，下载形态改为按需流式（详见 RFC 顶部状态注解） |

> 状态值约定：`Draft` → `Accepted` → `Implemented` → `Superseded` / `Withdrawn`。`Partially Implemented` 表示主线已落地、部分子目标仍开放；`Withdrawn` 表示已落地后又下线（替换为更轻方案）。

## 写一份 RFC（最小模板）

新建 `docs/rfcs/<NNNN>-<slug>.md`，编号取**当前最大编号 + 1**，slug 用 kebab-case。

```markdown
# RFC NNNN — <标题>

| 状态     | **Draft**（YYYY-MM-DD）                             |
| -------- | --------------------------------------------------- |
| 作者     | <你的名字>                                          |
| 创建于   | YYYY-MM-DD                                          |
| 影响范围 | 说明是否破坏性、是否需要数据迁移、影响哪些表 / 接口 |
| 关联     | RFC NNNN §X · getting-started/architecture.md §Y    |

---

## 1. 背景与目标

讲清楚现状、痛点、要解决的问题、要达成的指标。**不要直接写解决方案。**

## 2. 设计选项对比

至少列 2 个候选方案，每个方案给出"优点 / 缺点 / 适用场景"。这是 RFC 价值最高的部分。

## 3. 选择的方案

明确选 X，并说明为什么 X 比其他方案好。

## 4. 数据模型 / 接口契约

schema diff、新增 endpoint、Spider 接口签名等结构化变化。

## 5. 迁移与向后兼容

旧数据怎么办？灰度怎么放？回滚策略是什么？

## 6. 风险与开放问题

还没想清楚的、需要团队讨论的点。
```

## 上下游

- RFC 落地后，**必须**回填到 [`../getting-started/architecture.md`](../getting-started/architecture.md) 和 [`../reference/`](../reference/) 对应位置——RFC 留下"为什么"，那两处描述"现在是什么"。
- 路线图里的 Phase 拆解 → [`../getting-started/roadmap.md`](../getting-started/roadmap.md)
