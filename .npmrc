# pnpm 配置

# 自动安装 peer dependencies（pnpm 8 起默认开启，这里显式声明）
auto-install-peers=true

# 不因 peer 版本不匹配就报错——开发期更宽松
strict-peer-dependencies=false

# 不做扁平化提升，保持依赖图诚实
# 如果某个包坏掉是因为它假设了扁平 node_modules，可以放开下面这行
# shamefully-hoist=false

# 保存为非精确版本（带 ^），便于补丁版本自动升级
save-exact=false
